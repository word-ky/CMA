# 未来两条合法路径（本轮均不执行）

## 选项1：冻结当前w15并忠实解释

保持源码、checkpoint和Cycle025结果不变。解释为REF影响后续SEG语义，输出侧通过矿工mask/bbox显式定位；输出ref_hidden_fcs读到的是pre-REF共享上下文，另有真实REF辅助重建与rank训练。本轮只完成认识与教学修订，不把旧论文全文重写。

## 选项2：将来研究正确对齐的新模型

如果用户恢复科学开发，可让输出语义context读取真正未移位REF hidden，定义新的配置/模型名，在匹配训练设置下重新训练，再与冻结w15比较。确切历史训练manifest仍缺失，设计匹配条件时必须承认这一点。仅在推理时换索引会改变现有ref_hidden_fcs训练时所见的位置分布，不能作为原w15有效修复或公平性能结论。

不能机械地把SEG也取消移位：SEG预测位置的语义用法与REF注入后读取目标不同。也不能从索引偏移推出其造成了多少性能损失，或保证新对齐一定更好。

本轮没有候选patch被应用，没有新checkpoint、实验、训练、推理、ablation或scoring。
