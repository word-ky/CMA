# Cycle035 — causal-identifiability teaching and no-run design

REVIEW034/90b95a4 and Issue1 comment5740516455 implemented. Five requested documents: CAUSAL_IDENTIFIABILITY.md, MINIMAL_INTERVENTION_GRAPH.md, MATCHED_ABLATION_FEASIBILITY.md, FUTURE_CROP_ABLATION_PREREG.md, TEACHING_SUMMARY.md.

The seven-row table distinguishes observed joint-bundle control from unidentifiable component effects and training-dependent questions. The single proposed contrast zeros pooled crop features before bbox addition; full-image appearance and supplied bbox/mask localization stay available. Current pre-REF output indexing stays unchanged.

One exact common starting candidate is hash-bound w15 for BOTH newly continued arms, never historic w15 as the matched full arm. This estimates conditional continuation effects and retains prior crop-training/exposure limitations. The existing reconstructed 13,455-row manifest is hash-bound, but its executable shared restored-asset mapping is not established. Future budget/warmup and effective seeded launch configuration remain explicit TBD. Validation selection is disabled; existing Cycle025 is a known descriptive evaluation set, not a new blind test. Preregistration is deliberately non-executable until those concrete fields are resolved in a future authorized experiment; no new split selected.

Metadata/source inspection and document checks only: zero model/scorer calls, zero weight/image/prediction loads, zero production patches, zero scientific-result changes. No manuscript or Layer2 work. audit_receipt.json records the inspected source hashes and existing checkpoint/manifest identities.

Exactly one next recommendation: decide whether incremental crop-feature value conditional on the already crop-trained w15 and fixed supplied localization is the scientific question worth testing; do not interpret it as pure semantic memory attribution or from-scratch necessity.
