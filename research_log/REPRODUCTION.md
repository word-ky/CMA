# 固定配置复现结果

完成日期：2026-09-17。单张 RTX A6000，任务正常退出。此次发布只打包代码与已有结果，没有重新训练或评测。

| 指标 | 2026-05-28 归档 | A6000 恢复复现 |
|---|---:|---:|
| 样本数 | 30 | 30 |
| Accepted | 14/30 | 14/30 |
| 目标 mean IoU | 0.4013989366 | 0.4014485360 |
| Anchor mean IoU | 0.7478811950 | 0.7487211295 |
| 非法动作数 | 0 | 0 |
| 平均步数 | 14 | 14 |

固定配置：LISA++ MR v2b w15、Qwen2.5-0.5B-Instruct + SFT-v2、v3-lowseg 增强器；target15_b 为 gamma=2.15、scale=0.405、contrast=0.655、noise_sigma=23.5、blur=0.88；ROI target/anchor/ref_target=1.2/1.5/1.2；两个 IoU 接受阈值均为 0.5；max_steps=14，BF16。

全部 11 个 w15 模型文件与本地备份的 SHA-256 一致。30 条样本 ID、顺序、最终接受/失败判定和策略动作序列与历史归档一致。单样本最大绝对 IoU 差为 0.0381933，均值接近不代表逐样本完全一致。

原评测掩码未找到；原图从记录的数据归档恢复，掩码使用归档 SAM-B 流程重新生成。无法验证掩码逐像素一致。数据主要为伪标签；holdout30 曾用于开发和合成退化标定。

执行器仍使用 GT IoU 选择最佳候选和判断接受、回退；30 个样本执行同一 14 步序列，均进入 `max_steps_best_target`。因此该结果是历史研究协议复现，不是无标注在线自主规划的性能证明。此次没有修改权重、调整阈值或以复现分数拟合掩码。

机器可读指标见 [reproduction_summary.json](reproduction_summary.json)。移除了其中的服务器绝对路径，其余字段保留原汇总值。
