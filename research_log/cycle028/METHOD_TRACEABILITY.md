# Cycle028 implementation-to-manuscript map

Source revision: `73f07c7109144f3c80d2cb017c66e8935c19474e`. Paths below are repository-relative; line anchors refer to this revision. `source_receipt.json` preserves source hashes. This is source inspection, not an experiment.

## Mechanisms

| Manuscript mechanism | Exact source anchors | Implemented behavior and evidence status |
|---|---|---|
| Supplied entity memory | `cmllm_remote/scripts/eval_mr_ref_counterfactual_v0.py:73`, `norm_bbox`; `:84`, `make_ref_crop_clip`; `:202`, `build_item` | Mask the RGB image outside the supplied miner, crop the supplied bbox, zero-pad to a square and CLIP-preprocess; normalize bbox coordinates. Cycle025 uses the current condition for both full image and reference appearance. Implemented; whole-system evidence only; no matched component ablation. |
| Input-side REF conditioning | `cmllm_remote/third_party/LISA/model/LISA.py:732`, `build_ref_input_embeddings`; `:764` mean visual tokens plus bbox MLP, then REF MLP; `cmllm_remote/third_party/LISA/model/llava/model/language_model/llava_llama.py:91`, `_replace_ref_input_embeddings` | Add 0.5 times the reference vector at the expanded `[REF]` position (`gated_add`). The name denotes a configured scalar here, not a learned reliability gate. Implemented, not independently ablated. |
| Output-side SAM prompting | `cmllm_remote/third_party/LISA/model/LISA.py:429` REF-associated hidden extraction/projection; `:828`, `build_ref_prompt_embeddings`; `:894` mask downsampling; `:918` context sum; `:479` construction and `:496` fusion branch; `:513` SAM prompt encoder | Bilinearly resize supplied mask to 16x16, flatten and concatenate normalized bbox, apply geometry MLP, add projected REF-associated hidden state, cap context norm at 20, multiply learned `ref_embedding_scale` and validity. Configured `add` mode adds context to the SEG-derived text prompt; SAM decoding uses one mask output. `sam_prompt_embed_clamp=50`. Implemented, not independently ablated. |
| Reference-mask reconstruction | `cmllm_remote/third_party/LISA/model/LISA.py:474`, `get_ref_token_embeddings`; `:565` reference decoding; `:670` valid-reference BCE/Dice; `:699` weighting | Auxiliary decoder reconstructs the supplied reference mask from REF-token embeddings. Config stores `ref_reconstruction_loss_weight=1.0`; reference supervision is part of the recorded design, not an autonomous predicted-memory update. Implemented/config-supported; exact historical execution provenance is incomplete; not independently ablated. |
| Paired counterfactual construction | `cmllm_remote/scripts/build_mr_ref_counterfactual_train.py:54`, `build_counterfactual_episode`; `:32`, `split_bucket`; `cmllm_remote/third_party/LISA/utils/mr_ref_seg_dataset.py:209`, `_getitem_counterfactual` | Same-image pair IDs share `same_round2_query`; at least two valid pairs are retained. Dataset emits alternating miner/helmet masks and valid REF only on helmet rows. Historical path-MD5 split is not evidence of image-byte disjointness. Implemented; exact w15 run-bound input manifest UNKNOWN. |
| Segmentation and language objectives | `cmllm_remote/third_party/LISA/model/LISA.py:613` CE; `:644` weighted BCE/Dice and `:696` normalization | Weighted token CE and per-mask BCE/Dice, with sample/mask weighting and separate reference normalization. Historical run-specific CE/BCE/Dice coefficient overrides UNKNOWN; do not substitute launcher defaults. Implemented, not independently ablated. |
| Counterfactual identity objective | `cmllm_remote/third_party/LISA/model/LISA.py:84`, `soft_iou_matrix`; `:652` alternating helmet selection; `:667` hinge; `:711` normalization | For eligible grouped masks (`>=4`, even count), helmet logits/targets are `1::2`. Sigmoid soft IoU uses denominator clamped at 1e-6; each correct diagonal must exceed maximum off-diagonal by margin. Config weight 1.5, margin 0.05. Historical May16 log independently identifies w15 as the 1.5 branch. Implemented/config-supported; no isolated causal attribution. |
| Frozen Cycle025 inference | `research_log/run_cma_cycle025.py:37` checkpoint/hash checks; `:42` load; `:43` zero-target hook; `:50` group loop; `:52` build with `read_targets=False`; `:62` prediction; `cmllm_remote/scripts/run_mcr_train_predictions.py:21`, `load_base_w15`; `cmllm_remote/scripts/eval_mr_ref_counterfactual_v0.py:342`, `predict_item` | BF16 base-w15, `v1_multiround`, reference crop mode, seed 0, clean/target15_b. Fixed conversation includes SEG responses; direct model forward, not a generated autonomous dialogue. Helmet rows selected at `2*i+1`, logits thresholded at `>0`. Masks passed as supervision placeholders are zero; no helmet-target raster read or GT candidate selection. No enhancer, controller, spatial boxes or local-evidence adapters activated by this runner. Whole-system primary result supported by Cycle025 freeze/receipts. |
| Method-native comparator memory | `cmllm_remote/external_baselines/segllm/memory_state.py:19`, `seed_memory`; `research_log/cycle025/protocol_receipt.json`, pinned revisions; `research_log/run_cma_cycle025.py:57` pixel equality check | Same underlying condition image/query and corresponding supplied miner identity, encoded through different native memory interfaces. Do not claim identical internal reference tensors or training exposure. System-level comparison only. |
| Offline evaluation metrics | `cmllm_remote/scripts/eval_counterfactual_memory_fidelity.py:15`, `iou_matrix`; `:37`, `score_masks`; `:103`, `summarize`; `research_log/score_cycle025.py` | Binary target IoU matrix; strict diagonal dominance is Fidelity; CMSA requires both references to dominate and reach IoU >=0.5; IER requires stronger wrong IoU >=0.5; margin is diagonal minus strongest wrong. Reference-weighted mIoU/Fidelity/IER/margin, pair-weighted CMSA. These are evaluation definitions, not the training soft-IoU objective. Existing frozen outputs only; scorer not executed this cycle. |

## Configuration provenance

`W15_CONFIG_SNAPSHOT.json` is a byte-preserving copy of the original backup's w15 `config.json`. SHA256 is `2713d1a90022fa0e511063afbba73a269e0846e22a2d8f735931562aa36b28f3`, equal to `research_log/cycle021/inference_provenance.json:model_files_sha256.config.json` and the config hash in `research_log/cycle025/cma_prediction_freeze.json:checkpoint_provenance`. The Cycle025 runner verifies those model-file hashes before loading.

| Field | Verified checkpoint value |
|---|---|
| `ref_injection_mode`, `ref_input_scale` | `gated_add`, 0.5 |
| `ref_prompt_mode`, `ref_mask_pool_size` | `add`, 16 |
| `ref_reconstruction_loss_weight` | 1.0 |
| `counterfactual_rank_loss_weight`, `counterfactual_rank_margin` | 1.5, 0.05 |
| `ref_context_init_scale` | 0.2 (initialization only; not the learned final scale) |
| `ref_prompt_max_norm`, `sam_prompt_embed_clamp` | 20.0, 50.0 |
| Historical run-bound manifest, exact CE/BCE/Dice overrides, ancestor/pretraining exposure | UNKNOWN |

The launcher `run_lisa_finetune_mr_ref_v2b_rank.sh:28` defaults to rank weight 0.5; it is not the w15 frozen value. `research_log/cycle023/historical_log_excerpts.json:27` reports the completed 1.5 continuation branch. Neither checkpoint metadata nor that log recovers the missing exact historical run-bound training manifest. Learned scale values are not read or guessed here.

## Empirical boundary

Only whole-system supplied-memory performance is empirically supported by Cycle025. The main code's optional later branches do not become part of the frozen method simply because they are present. No independent, configuration-matched ablation is asserted for crop conditioning, geometry prompting, reconstruction, or rank loss. Primary evidence is DOCUMENTED_PROTOCOL_DISJOINT_CONFIRMATION, not exact training-unseen proof. Frozen Cycle027 Results/Limitations and Cycle026 claim ledger remain controlling sources for empirical wording.
