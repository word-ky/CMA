# Cycle034 — memory-interface parity teaching audit

REVIEW033/1e7b1e5 completed. Zero model/scorer calls, weights/images/predictions loaded, production edits, scientific-result changes, manuscript work or Layer2 work.

- MEMORY_INTERFACE_PARITY.md compares raw RGB/query/miner annotations against derived crops, coordinates, geometry and native history. Shared semantic query is reformatted for SegLLM; native text/tensors are not called identical. CMA has a separate16x16 miner-mask geometry prompt path; SegLLM's frozen port passes masked appearance+bbox after crop construction, with no additional full miner-mask field. Both still receive localization information.
- CMA_VS_SEGLLM_CAUSAL_MAP.md traces the two native routes with source anchors. Read-only inspection confirmed the remote SegLLM HEAD matches4593a069 and inference_cli:226–242 replaces appearance/bbox slots. No model was invoked. CMA retains the corrected pre-REF/shared-context interpretation.
- INPUT_CONTRACT_RECEIPT.md plus audit_existing_receipts.py/input_contract_check.json: PASS_METADATA_ONLY over50 groups/200paired trial keys per method. All200 SegLLM injected appearance/bbox hashes match construction receipts; RGB/source identity and fixed clean/degraded geometry match. Both prediction freezes precede recorded scoring. Runtime raster paths remain within the target-free allowlist. Asset preparation did read pseudo-targets before inference; no global “never read targets before freeze” claim is made.
- PARITY_TEACHING_NOTE.md separates raw-information parity, representation parity, system-level results and component attribution. Three appearance-only/bbox-only/mask-geometry-only variants are thought experiments only, with residual information explicitly acknowledged.
- NEXT_SCIENTIFIC_DECISION.md proposes one future matched two-arm comparison: preserve all supplied localization, remove crop pooled features consistently in training/inference in one arm. Both arms would require matched documented training; comparing a new arm directly against historic w15 is not automatically matched. No experiment, checkpoint or training-data selection performed.

Exactly one future scientific recommendation: if the user explicitly resumes experiments, discuss the matched fixed-localization/crop-feature ablation to measure incremental appearance-channel value, without claiming pure semantic attribution.
