# 一个未来实验建议：保留定位，匹配地移除参考外观特征

状态：**仅提案，未授权执行**。没有创建数据split、checkpoint、训练任务或修改生产源码。若用户继续学习，保持现状。

若用户明确恢复科学开发，最有信息量的最小问题是：**在完全相同的supplied mask/bbox定位条件下，参考crop特征是否提供额外收益？**

只设计一个两臂匹配实验：两臂从同一可核验初始化开始，用同一显式训练manifest、预算、loss系数与随机设置训练。完整臂保留crop视觉特征；定位保留臂把REF输入构造中的crop pooled feature设为固定零向量，保留输入bbox MLP、输出16×16 mask+bbox几何、相同prompt结构及其余损失。干预须在训练与推理一致应用；不只在冻结w15推理时拔掉crop。

沿用已冻结评估协议的图/query、身份、clean/target15_b及评分定义，不按50组结果调参或换样。原w15作为历史系统背景；因原始训练manifest/optimizer状态未知，不能把新训练的定位臂与旧w15直接充当训练匹配的两臂。该50组已被多次分析，也不能重新宣称为未接触的最终独立test。

这个设计衡量**在定位信息不变时crop-feature通道的增量作用**，并非证明“纯身份语义”的因果价值：crop含轮廓和裁剪结构，主图还提供外观，其他相关信息未被解耦。若有差异，只能在此训练/协议内归于这一明确干预；若无差异，也不证明所有语义memory无用。

不同时修REF索引、不改rank、不更换baseline或加入agent。否则无法把差异归于这一通道。待用户明确恢复实验后再确定可核验初始化和训练manifest，本轮不作选择。

唯一建议：将上述“固定定位、匹配移除crop-feature”作为将来恢复科学开发时优先讨论的单一组件实验。
