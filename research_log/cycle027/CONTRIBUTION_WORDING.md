- **Problem formulation:** We formulate counterfactual identity-memory relational perception for visually similar workers and small PPE targets, asking whether a supplied worker identity determines the corresponding equipment segmentation under a fixed observation and query.
- **Method and evaluation:** We operationalize a controlled intervention that switches supplied entity memory while preserving the observation and semantic query, and measure identity-dependent segmentation with CMSA, Memory Fidelity, Identity Error Rate and mIoU.
- **Empirical evidence:** We provide a frozen system-level comparison under a fixed coal-mine-style compound degradation, with Cycle025 as the primary DOCUMENTED_PROTOCOL_DISJOINT_CONFIRMATION. CMA shows stronger absolute degraded segmentation and identity fidelity than pinned SegLLM; exact historical exposure remains unknown, and we claim neither lower degradation sensitivity nor exact training-unseen generalization.
