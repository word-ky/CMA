# crop通道：从存在到测得依赖

1. **模型拥有crop feature不等于模型依赖它。** 以前只有代码路径；本次固定w15、图像、问题、bbox和mask，只把池化crop特征置零，直接观测输出变化。
2. **这次测到了什么？** 原图mIoU88.28%→39.99%、CMSA92%→4%；退化图67.53%→34.28%、CMSA58%→0%。保留定位和主图后仍大幅下降，说明当前冻结w15的功能表现强烈依赖这条crop特征通道。两个条件方向一致，没有把它们合成单一分数。
3. **为什么仍不能证明重训后必要或纯语义价值？** 模型原来带crop训练，突然输入零特征会改变输入分布；重训可能学会让其它通道补偿。crop本身也编码了mask形状，并非纯外观语义。主图仍有外观，bbox和mask仍有定位。这次没有训练新模型、没有证明自主记忆或一般退化鲁棒性。
4. **是否值得考虑matched continuation？** 现在有明确的大幅功能下降证据，值得下一轮评审讨论Cycle035的匹配继续训练是否值得成本，用以区分当前依赖和训练后补偿。仍需解决共同训练资产和预算绑定；从w15开始的两个分支只能回答既有训练历史下的增量价值。当前并未授权或执行训练。
