# Cycle029 paper positioning and input contract

Review028 / `2bd4f33` controls this integration. No scientific evidence changes.

| Term | Controlled meaning |
|---|---|
| Supplied identity-localized entity memory | Reference appearance crop + supplied miner mask + bbox. The worker is already localized externally. |
| Reference appearance | Derived from the same condition image as the main observation; not an independent clean historical frame. |
| Relational target | Helmet associated with the supplied miner identity. |
| Counterfactual intervention | Hold scene/condition and semantic query fixed, switch supplied memory A/B; intended helmet identity changes. |
| Evaluated degradation | Fixed `target15_b` photometric/noise/blur stressor: gamma darkening, contrast/scale reduction, Gaussian noise and Gaussian blur. No dust/glare/occlusion simulation. |
| Primary evidence | Cycle025 DOCUMENTED_PROTOCOL_DISJOINT_CONFIRMATION; reconstructed pseudo targets; exact historical run-bound/ancestor exposure UNKNOWN. |
| Comparator | Pinned SegLLM, system-level comparison using method-native memory interfaces; same underlying observation/query and corresponding supplied identity information. |
| Scope | Supplied-memory-conditioned relational segmentation. No autonomous memory acquisition/writing/tracking/repair, exact training-unseen proof, lower degradation sensitivity, architecture-only superiority, successful Layer-2 agent/controller or safety guarantee. |

The new title, abstract and method figure use this vocabulary. Existing Cycle028 Introduction/Method and Cycle027 Results/Limitations remain byte-unchanged and are included directly. A short integration note immediately after the Introduction explicitly limits its broader dust/glare motivation to context and names the actual tested operations. A results protocol paragraph specifies the same input contract and comparator interface. No new bibliography/novelty search is performed.

The method overview is a vector schematic, not an additional qualitative case or a claim that all predictions switch correctly. Its desired A/B outputs explain the intervention; measured successes/failures remain in the frozen main table and qualitative bank.
