# Frozen inference dependence: implementation contract

Task: REVIEW035/6b63b65, Issue1 comment5740754378. Only crop-zero arm; no training. Production code unchanged.

`crop_zero_hook.install` reads the actual loaded `build_ref_input_embeddings` method, parses its AST and inserts one assignment immediately after the unique `crop_features = self.encode_images(ref_images).mean(dim=1)` assignment. The inserted evaluation-only call returns `torch.zeros_like(crop_features)`. The original bbox projection, addition, REF projection, valid-row handling and every other statement remain intact. It binds the altered function to this model instance only. Receipt records original/transformed method hashes and shapes/dtypes per invocation. Full-image encoding calls outside this method remain unchanged.

`run_crop_zero.py` reuses `research_log/run_cma_cycle025.py`: unchanged data preparation, zero-target hook, tokenizer, model loader, original-resolution output and binary threshold. Changes are output directory, pre-run binding checks and hook installation/receipt. No baseline inference rerun. Checkpoint files are rehashed against the Cycle025 control, source kernels against its protocol; current main-model sources are bound to local normalized-text hashes. BF16, seed0, 50 groups, clean and target15_b unchanged. Physical GPU1 is selected for available memory, same A6000 model; not a timing experiment.

Before inference, check frozen control prediction bytes, identity manifests, runtime RGB/miner-mask assets and execution manifest. During inference only permitted inputs are read; target tensors remain zero. Before scoring, freeze/hash all200 new masks, verify control/new hashes, input read allowlist and full identity order. Scoring reuses the unchanged CMF evaluate_file(min_iou=.5); existing full-arm metric reports are read rather than regenerated. Existing pseudo-target preparation was not target-blind.

Tests: crop/bbox arithmetic and inserted AST toy forward PASS in float32/BF16; values, shape, dtype and device preserved. The toy is software evidence only. Existing scorer/export tests15 passed16.04s, full cmllm_remote suite54 passed27.69s. Static compilation passed. First remote launch failed before Python due CRLF shell lines, corrected to LF without scientific changes; failed run20260919-173909-cma-cycle036-crop-zero preserved. Actual execution run20260919-174012-cma-cycle036-crop-zero.

Interpretation fixed before results: loss after zeroing indicates learned crop-channel dependence conditional on retained localization; small/null change indicates weak dependence here, not universal irrelevance; mixed clean/degraded effects stay separate. No component retraining, pure-semantic attribution, new blind-test or autonomous-memory claim.
